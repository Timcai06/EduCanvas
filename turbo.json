{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": [
        "ASSET_STORAGE_ROOT",
        "DATABASE_URL",
        "EDUCANVAS_*",
        "MODEL_GATEWAY_*",
        "OBJECT_STORAGE_ROOT",
        "PORT",
        "SEARCH_*",
        "TELEGRAM_*"
      ]
    },
    "lint": {},
    "test": {
      "dependsOn": ["^test"],
      "outputs": []
    },
    "test:integration": {
      "cache": false,
      "env": ["TEST_DATABASE_URL"],
      "outputs": []
    },
    "typecheck": {}
  }
}
