# 类型安全规则

- 禁止在源码中使用 `any` 类型。如果类型确实不确定，用 `unknown` + 类型守卫。
- 禁止使用 `as` 类型断言绕过编译检查。唯一例外：测试中的 mock 数据构造。
- 可选链 `?.` 的返回值默认可能为 `undefined`，必须显式处理该情况。
- Zod Schema 的数量/长度上限必须在文件中注明原因（项目规范要求）。
- 错误捕获中使用 `catch (error: unknown)` 而非 `catch (error: any)`，并做类型缩窄。
- 导出 API 必须有中文 JSDoc 说明调用边界、重要不变量或使用限制。
