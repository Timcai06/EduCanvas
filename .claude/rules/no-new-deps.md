# 依赖管理规则

- 禁止新增 npm 依赖，除非用户明确要求。
- 如果确实需要新的依赖，必须先说明：为什么不能用现有依赖解决、该包的许可证是否兼容、包大小和依赖深度。
- 禁止使用 `npm install` 或 `yarn add`，只允许 `pnpm add`。
- 安装新依赖后必须跑 `pnpm install --frozen-lockfile` 验证 lockfile 一致性。
- 不要因为"只用一个函数"引入一整个工具库。优先手写或用已有依赖的等效功能。
