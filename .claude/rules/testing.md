# 测试规则

- 修改任何 .ts 源码前，先补一个会失败的测试（TDD：红→绿→重构）
- 测试必须断言用户可见行为，不能只断言「函数被调用过」
- 不允许大面积 snapshot 测试，用具体的 expect 断言
- 修改后必须跑目标文件的 vitest，退出码 0 才算完成
- 涉及时区的测试必须在 TZ=UTC 和 TZ=America/Los_Angeles 两种环境下通过
- mock 数据不能让空档案看起来已有学习记录
