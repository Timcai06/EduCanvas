# 第二代 Hybrid Ports 架构升级

- 状态：`active`
- 负责人：项目负责人
- 最后验证时间：2026-07-22
- 对应路线图阶段：[项目路线图](../../10-planning/roadmap.md)
- 架构决策：[ADR-0020](../../09-decisions/0020-第二代HybridPorts架构.md)
- 前置研究：[第二代架构研究结档](../completed/2026-07-第二代架构研究.md)

## 目标

在保持 Web-first K12 产品、Notebook/身份边界、教育可信事实和现有数据可回滚的前提下，把 Gateway、Web General、Web Teaching 三条 Turn 组合路径和两套 Tool Runtime 收敛为：一个 Gateway Control Plane、一个 Turn Application Service、一个 Context Engine、一个 AgentLoopEngine、一个 Tool Kernel，以及以教育为默认 Profile 的确定性 Domain Services。

## 范围

- 冻结并实现 transport-neutral `TurnApplicationPort`、Context/Ledger/Tool/Continuation/Trace Ports；
- additive 泛化 Turn Ledger，使通用与教学 Turn 共享 Model Run、Tool Call、Context Snapshot 审计语义；
- 生产化 Tool Kernel 和 effect ledger，适配 Local、Teaching、Node、MCP；
- 按 Gateway → Web General → Web Teaching 顺序迁移组合根；
- 实现审批 continuation、跨进程 lease/cancel、effect reconciliation 与真实脱敏 Trace；
- 将 AI SDK、MCP v1、OpenTelemetry 限制在可开关 Adapter，保留 native/旧 Adapter 回滚；
- 删除已经无生产调用者的重复 Runtime，回写架构、数据、接口与运维文档。

## 非目标

- 不重排 `apps/ + packages/` 顶层目录，不拆微服务；
- 不让普通 Turn 进入 LangGraph，不引入 Temporal/Redis/Kafka；
- 不复制 OpenClaw 全部 provider/channel/plugin，也不复制单用户宿主机信任模型；
- 不向未成年人默认开放 Shell、任意文件系统、Credential 或设备写能力；
- 不在本阶段物理删除旧教学表；停止写入与保留期验证后另立清理决策。

## 不变量

1. Gateway 是认证路由、Operation、审批、事件与投递的唯一控制面；
2. 正常 Turn 只有一个生产 `AgentLoopEngine` 组合点和一个应用服务；
3. Operation Event、Turn Ledger、Workflow Checkpoint、Worker Job、Trace、学习事实不互相冒充；
4. Tool 有效能力是 Actor、Agent、Notebook、Profile、入口、环境、安全策略的服务端交集；
5. Notebook Membership 不传播 Memory、Credential、Node 或 default grant；
6. Schema additive、入口单写迁移、旧 ID 可追溯、Adapter 可回滚；
7. 模型、客户端、MCP annotations、checkpoint 和 Trace 都不能写可信学习事实。

## 验收条件

1. 生产源码只有一个 Turn Application 实现和一个 `AgentLoopEngine` 构造位置；Gateway/Web General/Web Teaching 都调用它；
2. Web SSE 与 Gateway Event 对同一 scripted Turn 产生等价文本、工具、取消、错误和唯一终态；
3. Local/Teaching/Node/MCP Tool 都经过同一 Tool Kernel，覆盖五维权限交集、L2/L3 审批、幂等、timeout/cancel、`outcome_unknown` 和 secret 零外泄；
4. 通用与教学 Turn 都写入统一 Model Run、Tool Call、Context Snapshot 契约；学习事件继续引用可信证据，旧记录可读；
5. approval 通过后可跨进程继续；五个 kill point 无重复副作用、无双终态，N-2/N-1 与回滚 fixture 通过；
6. 跨进程 cancel/lease 在进程退出和重领后得到唯一 cancelled/failed/completed 终态；
7. `operationId + traceparent` 串联入口、Gateway、Turn、Model、Tool、Worker；默认遥测和队列不含正文、Prompt、判分键、Token、Secret、Credential、对象 key；
8. 共享 Notebook 中各 Actor 仍使用各自 Personal Agent，私人能力跨 Actor 全部 fail closed；
9. AI SDK/native Provider parity、MCP 生命周期与恶意输出、OTel exporter degraded/关闭路径均可验证且可回滚；
10. `make check`、`make integration`、生产 build、E2E、PTY smoke、Web/TUI 同 Notebook/handoff/审批/cancel 全绿；canonical 文档与实际代码一致。

## 实施阶段

### M0：接受决策与冻结 Ports

- [x] Code Owner 接受 ADR-0020；
- [x] 在 `agent-core/agent-runtime` 定义版本化 Turn input/event、Profile hooks、Context、Ledger、Tool、Continuation、Trace Port；`educanvas.turn.v2`与`educanvas.operation-continuation.v1`已定型，Continuation只暴露稳定引用、活动唯一性和generation fencing，不携带业务事实或敏感payload；
- [x] 添加依赖边界与生产构造点扫描测试；三条生产调用路径固定为`TurnApplicationService`，`AgentLoopEngine`源码构造allowlist已收紧为唯一位置，旧工具与教学Runtime不得重新引入；
- [x] 用 scripted golden fixture 固定 Web/Gateway projection 的文本、引用、工具、失败、取消与唯一终态等价性。

### M1：统一 Ledger 与 Context Engine

- [x] additive 泛化 `model_runs`、`tool_calls`、`turn_context_snapshots` 对通用 `agent_turn` 的约束和归属；三张表均已完成双形状迁移并保留旧教学读取；
- [~] 实现统一 Ledger repository、lease、replay/idempotency 与旧记录兼容读取；Model Run、Context Snapshot 与 Tool Call Ledger Port/仓储已实现 Actor 复核、Operation 幂等、上下文漂移/双唯一键冲突拒绝和旧教学记录升级证据，Teaching Profile已复用同一Operation审计并保留K12消息lease；跨进程重领待M4；
- [x] 实现 Context Engine：确定性Segment预算、必需Profile、Conversation、Source/Asset、Memory unavailable、tool-pair preservation与版本化Snapshot material已完成，Gateway、General与Teaching均已生产接线；
- [~] 完成数据迁移、N-2/N-1、跨 Actor、删除/最小化和回滚测试；三类账本已覆盖 N-1、跨 Actor 和不保存正文，N-2、整体验收与旧路径停写后的回滚证据待办。

### M2：生产 Tool Kernel

- [x] 将研究 fixture 定型为生产 Tool definition/Adapter/Policy/Execution 契约；四类Adapter共享五维能力交集、L2/L3审批门、Schema、timeout/cancel与稳定失败语义，执行前已取消会在Adapter与effect intention之前记审计失败并停止；L2/L3现按Schema→pending Tool Call→幂等耐久准备→approval.required挂起，不再写APPROVAL_REQUIRED失败终态；
- [x] 新增持久 effect ledger，按 Operation/Call/effect key 保证幂等与对账；write先记intention，唯一终态为committed/failed/outcome_unknown，账本不含原始值；
- [x] 适配通用 Local Tool、Teaching Tool与Node；Web General网页Tool、Web Teaching教学Tool以及Gateway/Web General的Node L0/L1只读能力均已切入生产Kernel和可信上下文，Node按Operation Actor/Agent与新鲜心跳授权且不接受模型提供nodeId；
- [x] 增加 MCP v1 Streamable HTTP Adapter：服务端可信注册冻结capability/risk/effect，远端`annotations`不参与授权；每次调用执行initialize、分页`listTools`、Schema防漂移、`callTool`与可靠关闭；输入、HTTP响应、文本和结构化输出均有界，媒体/资源诚实拒绝；Bearer必须经Actor/Agent范围Credential Broker解析不透明句柄，生产未接Broker时整组工具disabled；Gateway与Web General已接入无鉴权L0/L1工具，状态以稳定`disabled/idle/ready/degraded`与脱敏失败码表达；
- [x] MCP L2/L3高风险恢复：只允许write；参数与Credential Handle进入Adapter自有AES-256-GCM短期密文意图，公共continuation仍只有稳定引用；审批后由既有Worker恢复，外呼前擦除密文，重领时按Effect Ledger补齐已提交结果或收敛`outcome_unknown`，绝不盲目重放；缺密钥/仓储/公共Intent任一依赖时高风险能力fail closed；
- [x] 删除旧 `AgentToolRegistry`、`TeachingToolExecutor` 与 `TeachingTurnOrchestrator`；Local/Teaching只保留小型定义与Adapter模块，模型循环和工具生命周期只有统一Runtime一份实现。
- [x] 将生产 Tool Kernel 按公开契约、策略、审批、执行控制、副作用结算和调用生命周期拆为独立模块；顶层仅保留兼容导出，生产与测试模块均以250行为本地门禁，防止统一内核重新退化为不可读大文件。

### M3：统一 Turn Application 并逐入口迁移

- [x] 实现唯一 `TurnApplicationService`，组合 Context、Ledger、Loop、Tool、Profile hooks、取消与 Trace；核心服务、Provider校验后Model Run结算、Context/Prompt防漂移、Tool Kernel调用、replay单终态和遥测降级已完成，Profile preflight 与有界流式 OutputGuard 已在统一服务内提供 `POLICY_BLOCKED` 诚实终态；
- [x] 将唯一Turn应用流程按启动/重放会话、Context准备、Tool批执行、模型循环投影和Profile完成结算拆分；顶层Service只固定阶段顺序，生产与测试模块均以300行为本地架构门禁。
- [~] 迁移 Gateway runner，补齐 Asset/Tool/审计能力；文本Turn已迁入唯一服务并写统一Context/Model Run审计，同一Gateway trace贯穿，Operation终态改为Gateway单写，取消先持久化；Node Tool已与Web General共用Adapter，Asset仍待补齐；
- [x] 迁移 Web General，Web Route 只保留身份/BFF/SSE projection；Gateway Operation复用、结构化Asset Context、网页Tool/Effect、引用、重放、DB取消watcher与统一Ledger已接通，真实DeepSeek文本和`fetchWebPage`两圈工具纵切通过；
- [x] 迁移 Web Teaching，把状态机、判分、安全、掌握度保留为确定性 hooks/Domain Services；真实 Web→Gateway Operation→`TurnApplicationService`→DeepSeek 纵切已通过，answer/synthesis 两次 Model Run、`getStudentState`/`retrieveKnowledge` 两次 Tool Call、唯一 Context Snapshot、输入/输出安全允许决策与 K12 完成消息均归属同一 Operation，且该 Operation 的旧 `teaching_turn` Model Run 为零；
- [x] 每次只切一个入口并保留入口级回滚证据；三条生产调用路径已收敛，扫描测试证明源码无第二构造位置。

### M4：Continuation、跨进程取消与 Trace

- [x] 实现版本化 Operation continuation 状态、领取 lease、approval 后原子入队和恢复前重新鉴权；版本化账本、多等待点sequence、generation fencing、批准决策/事件/ready/job原子提交、Worker授权重算与原子终态已完成；Turn Application具备无终态审批挂起和Adapter耐久准备契约，`educanvas.tool-approval-intent.v1`将prepared意图与Gateway事件、Approval、continuation原子绑定，公共与MCP Adapter密文意图均有五分钟有界reconciliation；MCP高风险恢复已接入，Node仍只开放无需审批的L0/L1只读能力，未来高风险Node必须另经评审；
- [~] 实现 Tool effect reconciliation 与 write `outcome_unknown` 人工/自动对账边界；已建立追加式core契约、runtime决议服务与PostgreSQL仓储/Schema，原Effect、Tool Call和Operation终态不回写；自动verifier只允许查询并须匹配Effect intention中服务端冻结的身份，调用方不能换用其他核验器，MCP v1因无可信查询契约而fail closed；人工resolver只接受受信operator/service principal，学生不能自证；生产触发、具体Adapter verifier与Graphile Worker接线仍待后续独立PR；
- [x] 实现跨进程 cancel request、owner heartbeat、lease 回收和唯一终态；PostgreSQL请求事实覆盖等待态即时取消、运行态heartbeat/结算竞速，未过期lease强制Graphile重试，过期后generation换代重领；
- [~] 在稳定Trace Port后接入OTel：独立`packages/telemetry`已以默认关闭的Adapter注入Gateway/Web General/Web Teaching，属性/事件白名单、比例采样、有界Batch、OTLP HTTP和exporter degraded均有测试；真实Turn span的W3C `traceparent`已经审批意图和PostgreSQL continuation耐久传播到Worker active子Span，Graphile payload仍只含`continuationId`，carrier/Exporter失败不改变业务结果；入口、Gateway、Model、Tool独立生产Span与正式Collector/SLO仍待后续纵切；
- [x] 处理 graphile-worker 异常退出长锁：新增独立Recovery Port与每分钟有界维护任务，`SKIP LOCKED`扫描过期业务lease并在同一事务用稳定job key重投纯`continuationId`；扫描不改业务状态或generation，新任务仍经claim换代与全量重授权。低基数恢复/健康字段、告警阈值、受控补投Runbook和真实SIGKILL集成证据已补齐，MCP `outcome_unknown`保持不重放；

### M5：Provider、产品一致性与收口

- [x] 将原生Model Gateway按协议投影/校验、网络流生命周期、环境工厂和分域测试拆分，全部模块以400行为本地架构门禁，为双Adapter生产接线提供可读、可回滚边界；
- [x] 将AI SDK Adapter以`MODEL_GATEWAY_RUNTIME=ai-sdk`显式接入生产`TurnModelGateway`，native保持默认回滚路径且不静默fallback；SDK协议/流/Provider工厂独立成模块，Gateway与Web公共工厂接线，golden parity、恶意元数据、alias/输出限制/DeepSeek thinking闸门和真实双Adapter短请求均通过；
- [ ] Web/TUI/Channel 对相同 Notebook、Profile、Tool policy、approval、send/abort 和终态一致；Profile权威路由已收敛，Gateway不再硬编码或静默降级未知Profile。公共五维Tool Policy Resolver与Gateway General可信投影已完成：实际Adapter为共同上界，Actor/Notebook/Profile/Channel/Environment分别授权，入口manifest不能增权，Channel/System在审批与停止语义接通前保持空Tool能力。Web General的528行组合文件已纯机械拆成五个受300行门禁约束的职责模块；Web General策略接入、Web Teaching拆分与策略接入、共享General/K12装配、Channel abort及真实跨入口证据仍待后续纵切；
- [ ] 根据真实需求决定是否增加 steer；未增加时明确使用 cancel + contextual resend；
- [ ] 清理重复组合根、临时研究生产导入和过期文档，保留复现 fixture；
- [ ] 完成性能、成本、隐私、安全、可访问性、PTY、亮暗主题与完整服务 dogfood。

### M6：验收与结档

- [ ] 全量门禁、五点故障注入、数据升级/回滚、备份恢复和 secret 扫描通过；
- [ ] 清除当前main已有的两条general-chat E2E红基线并补齐稳定通过证据；本Tool Effect切片只记录该最终收口blocker，不在此PR修改UI；
- [ ] 架构、安全、代码质量三轮复核无阻塞问题；
- [ ] canonical 文档回写，研究结论与生产事实明确分层；
- [ ] 本计划压缩移入 `completed/`，路线图进入后续正式身份/生产运维与教育质量阶段。

## PR 切分原则

- 每个 PR 只完成一个可独立回滚的纵切；Schema、仓储、Runtime、调用方和测试尽量同 PR 闭环；
- 依赖顺序为 Contracts → Data → Kernel/Context → Gateway → General → Teaching → Continuation/Trace → Adapter/cleanup；
- 任何入口切换前先有 parity test，切换后保留上一 Adapter 至下一里程碑验证；
- 不使用长期 dual-write；若一次切换无法保证单写，停止并缩小纵切。

## 预期事实回写

| 稳定事实类型       | 目标文档                                                                                   |
| ------------------ | ------------------------------------------------------------------------------------------ |
| 产品跨入口行为     | `docs/01-product/product-definition.md`、`student-ui-spec.md`                              |
| 运行架构与 Gateway | `docs/02-architecture/01-系统架构现状.md`、`02-Gateway与多入口.md`、`03-第二代架构提案.md` |
| Agent/教育边界     | `docs/03-ai/01-Agent编排边界.md`、教学领域文档                                             |
| 账本与迁移         | `docs/04-data/data-design.md`、迁移说明                                                    |
| 协议与测试         | `docs/05-engineering/`                                                                     |
| 安全与隐私         | `docs/06-security/`                                                                        |
| 运维与恢复         | `docs/07-operations/`                                                                      |

## 验证证据

| 验收项                | 证据                                                                                       | 结果      |
| --------------------- | ------------------------------------------------------------------------------------------ | --------- |
| Ports 与唯一构造点    | 单测、依赖扫描、PR                                                                         | `pending` |
| Ledger/Context        | migration、DB integration、N-2/N-1                                                         | `partial` |
| Tool Kernel           | Local/Teaching/Node/MCP Adapter、真实SDK生命周期、effect、L2/L3耐久准备/恢复               | `passed`  |
| Gateway统一纵切       | 适配器单测、PostgreSQL integration、TUI→Gateway→DeepSeek实跑、统一Ledger查询               | `passed`  |
| Web General统一纵切   | Web SSE→Gateway Operation→统一Service、DeepSeek、Tool/Effect/引用DB核验                    | `passed`  |
| 三入口统一            | golden parity；Gateway、Web General、Web Teaching真实纵切、统一账本及35项E2E               | `passed`  |
| Continuation/cancel   | PostgreSQL/Graphile跨进程取消竞速与lease换代；五kill point待M6汇总                         | `partial` |
| Effect reconciliation | append-only决议契约/Schema/仓储；生产触发、Adapter verifier与Worker接线待办                | `partial` |
| Trace/隐私            | OTel Turn Adapter、Exporter degraded、Turn→PostgreSQL→Worker W3C生产传播；其余生产Span待办 | `partial` |
| Provider双Adapter     | native/AI SDK golden parity、边界扫描、DeepSeek双实现live smoke                            | `passed`  |
| 最终产品              | build、E2E、PTY、dogfood、文档复核                                                         | `pending` |

## 风险与回退

- **教学行为回归**：先冻结 Teaching golden transcript/可信事件，再迁 hooks；失败立即切回旧 Teaching Adapter；
- **账本双写/丢写**：入口级单写 feature flag，迁移只 additive，旧读路径保留；
- **副作用重复**：所有 write Tool 先建 effect key/ledger；不确定结果保持 `outcome_unknown`，后续只追加受信对账决议，不回写原终态或通过invoke试探；
- **跨进程竞态**：PostgreSQL row/advisory lock、lease generation 与唯一约束；真进程 kill 测试；
- **框架泄漏**：AI SDK/MCP/OTel/LangGraph 类型只在 Adapter；依赖边界测试阻止反向导入；
- **范围过大**：按纵切独立 PR；任何阶段必须在上一阶段全绿并可回滚后开始。

## 收尾检查表

- [ ] 验收条件均有证据，失败项已明确转入后续计划；
- [ ] 稳定事实已经回写到 canonical 文档；
- [ ] ADR-0020 的决定与实际实现一致；
- [ ] 重复生产路径和过期过程记录已经删除或压缩；
- [ ] 计划已经移入 `completed/`并更新索引。
