# RM 统一资源工作台交付与验收证据

- 状态：`WAITING_REVIEW`
- 基线：`49dcbe89902e57fc1753f9caa2b9514ea06f5245`
- Stage 2 起点：`c23b7249fa8c527dbb4033560d58090514947247`
- 最终候选：本文件所属提交；其 exact commit SHA 由 `RM_STATUS.md` 与下述最终日志首行共同冻结（避免文档自引用提交哈希）
- 验证环境：Node `24.18.0`；PostgreSQL 独立数据库；Playwright 标准 Chromium、移动 Chromium、Firefox 矩阵
- 交接标记：`RM_RX03_READY`（只表示已提交唯一审查者，不等于 PASS）

## 一、最终资源产品事实

- Resource Dock 与 Studio 资源库消费同一严格 `WorkspaceResourceSummary`；Dock 恢复工作集，资源库负责完整搜索、筛选、排序和 cursor 分页。
- Source 与 Artifact 通过同一 `CanvasResource` 门禁打开；Renderer 与 `allowedActions` 由服务端投影，执行时重新鉴权，摘要不是 capability token。
- Composer 的 `+` 只添加 Source；单一 `outputPreference` 随 Turn 提交。普通 Turn 与 Live 使用同一份冻结 Context Snapshot，不以发送后的 UI 变化替换版本。
- Artifact 的 proposed、generating、ready、failed、cancelled 使用同一 `artifactId` 状态卡；ready 后同一对象进入聊天、Dock、资源库和 Canvas，不生成第二张完成卡。
- Source 原件、结构化或降级表示按权威质量事实展示；正文、媒体和 Runtime 只在打开后加载，processing 状态有界轮询并诚实收敛。
- Live 内资源预览是同一 Workspace 资源事实的只读投影；关闭、返回或 Escape 不退出语音，资源失败不改变 ASR、TTS 或 Turn 状态。

## 二、RM07–RM09 功能证据

| 范围                       | 自动证据                                                                                                                      | 结果                                                                                         |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| RM07 Artifact 数据链与身份 | Worker Markdown generation、Artifact polling/generation flow、Turn reducer、消息卡、hydration、Dock integration focused tests | Web 全量 213 files / 1588 tests；Canvas Protocol 18 / 142                                    |
| RM08 Live 连续性           | `live-voice-resource-continuity.test.tsx`、preview/panel/stage、interruption/session/playback；Playwright Live 两场景         | `speech.abort` 与 Web Audio `playback.stop` 均先于 Turn cancel/resume；三浏览器 6 项预检通过 |
| RM09 规模与性能            | Dock/资源库 0、6、7、63、256、500 摘要；双页 cursor、去重、无详情读取、500 项投影和搜索 p95                                   | focused 25 项通过；本机 warmed p95 均低于 100ms                                              |
| RM09 无障碍                | Dock/资源库键盘、ARIA、loading/error/empty、窄屏滚动约束、reduced-motion；CA rAF 焦点恢复断言保留                             | 单元与 E2E typecheck 通过                                                                    |

## 三、RX01 权限与安全负例

- 跨 Notebook、跨主体、成员撤销后的 detail/mutation/media 均稳定拒绝；浏览器摘要中的旧动作不能绕过 fresh repository 鉴权。
- Artifact provenance 只携带不可变 Source 身份用于展示和导航；引用 foreign Source 不产生读取权限。
- Notebook/主体切换、资源关闭和目标变化会取消或忽略迟到响应；未知 Renderer、版本、动作和恶意响应 fail closed。
- Markdown 的危险 URL、data URL 和 raw HTML 不进入可执行 DOM；Generated Image URL 限制为同源 Artifact API 路径。
- Web App manifest 拒绝外链、绝对路径、路径穿越和 secret 字段；Bridge 绑定 source、origin、runtime、Notebook、版本、hash 与序列。
- 浏览器投影不包含 Prompt、Provider raw body、对象 key、凭据或 stack。

## 四、RX02 最终门禁

Stage 2 原始日志根目录：`/Users/tim/DEV/EduCanvas-coordination/logs/rm-rx02-20260813-0211`

RX03_REVISE exact-SHA 日志根目录：`/Users/tim/DEV/EduCanvas-coordination/logs/rm-rx03-revise-20260813`

| 门禁                   | 命令摘要                                                         | Exit | 结果 / 日志                                                                        |
| ---------------------- | ---------------------------------------------------------------- | ---: | ---------------------------------------------------------------------------------- |
| Web unit               | `pnpm --dir apps/web test`                                       |    0 | 211 files / 1571 tests；`web-unit.log`                                             |
| Canvas Protocol unit   | `pnpm --dir packages/canvas-protocol test`                       |    0 | 18 / 138；`canvas-protocol-unit.log`                                               |
| DB unit                | `pnpm --dir packages/db test`                                    |    0 | 修正受控 subpath 治理后 11 / 76；`db-unit-rerun.log`                               |
| Worker unit            | `pnpm --dir apps/worker test`                                    |    0 | 26 / 151；`worker-unit.log`                                                        |
| Typecheck              | Web、Canvas Protocol、DB、Worker、E2E 的 `typecheck`             |    0 | `*-typecheck.log`                                                                  |
| Lint / format          | `pnpm lint`                                                      |    0 | Turbo lint 4/4，仓库 Prettier 全绿；`root-lint-rerun.log`                          |
| File governance        | `pnpm file:check`                                                |    0 | 1971 tracked files classified；`file-governance.log`                               |
| PostgreSQL integration | `TEST_DATABASE_URL=... pnpm test:integration`                    |    0 | Worker 12 / 54；DB 50 / 340；`postgresql-integration.log`                          |
| Migration              | `node tooling/quality/migration-integration.mjs 49dcbe8... HEAD` |    0 | governance、records、drift、fresh/N-1 16/16，无生成物；`migration-integration.log` |
| Resource Playwright    | 标准 config，4 files、5 逻辑场景、3 projects                     |    0 | 15/15；`resource-playwright-accepted.log`                                          |
| Diff                   | `git diff --check`                                               |    0 | 最终提交前复验                                                                     |

### RX03_REVISE 修正与 exact-SHA 门禁

- Turn Artifact 保留唯一 `general-turn-artifact:<operationId>` 键；SHA-256 指纹绑定 versioned canonical request，覆盖文本的 kind/title/instruction/provenance 与图片的 kind/title/prompt/size。精确重试保持相同指纹，同 Turn 语义变化保持同键但产生不同指纹并由 repository 拒绝。
- held Live speech 先交付真实可播放 PCM、保持响应在途；插话证据同时要求 `speech.abort`、Web Audio `playback.stop`、active source 归零，并证明二者均早于 `turn.cancel` 与 resumed `turn.request`。
- failed/cancelled revision 不改变已有 immutable Artifact 的对象级 ready 事实；聊天与 Live 保持 active，Dock/Canvas 的 fresh `CanvasResource` 保留旧版本与动作，同时状态提示单独展示 revision outcome。首次生成失败仍为对象级 failed。
- Artifact observation 使用总时限、窗口数上限与退避；stuck job 返回公开 `timed_out`，消息卡已存在时仍显示可恢复提示。local observation abort 不冒充服务端 cancelled。
- 最终 exact-SHA 波次复跑 Web/Canvas unit、Web/Canvas/E2E typecheck、root lint/format、file governance、三浏览器资源矩阵与 diff/status；每份日志首行冻结同一候选 SHA。
- PostgreSQL integration 与 migration 复用 RX02 证据：`6e1904a..最终候选` 仅修改 Web、Canvas Protocol、E2E fixture/test 与资源产品文档，未修改 `packages/db/**`、Drizzle schema、migration 或 Worker persistence path。

## 五、失败归因与修正记录

| 类型       | 事实                                                                                                          | 处置                                                                                                     |
| ---------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| 代码门禁   | DB unit 首次 2 项失败：RM01 新受控 subpath 未进入 import-boundary allowlist                                   | `d6f3a99` 增加精确文件与符号白名单；全量 DB unit 复跑通过                                                |
| 格式门禁   | 根 Prettier 首次发现一个 RX01 route test 排版不一致                                                           | `38de863` 机械格式化；相关 33 项与根 lint 复跑通过                                                       |
| 测试编排   | Playwright 首次误带 `--` 导致文件过滤失效，且临时 E2E 库未迁移                                                | 第 2 个用例中止；重建 `_e2e` 库并迁移，以直接 Playwright 文件过滤运行                                    |
| 陈旧测试   | 已删除的 PlusMenu Artifact 创建入口与重复 Live 文本 locator                                                   | 资源专项改用当前 Source 入口与 Studio 资源库；最终矩阵覆盖当前产品路径                                   |
| 返修前失败 | 三浏览器 Live 插话场景 `speechAborts=0`；随后 `e125bef` 曾删除断言                                            | 旧失败日志保留为修前证据；fixture 改为在途 PCM，并增加真实 Web Audio stop 与严格顺序断言                 |
| 返修预检   | 首轮 Web 全量 3 项旧 adapter 断言仍把失败 revision 覆盖为对象级 failed；Live 预检误要求第 2 个 speech request | 更新为“已有版本 ready / 首次生成 failed”精确契约；held answer 等待第 1 个 speech request，未删除停止断言 |
| 环境       | 首次 `docker compose up` 与 reviewer 已在运行的 `educanvas-db` 容器同名                                       | 未停止或替换共享容器；在其上创建 RM 专用数据库                                                           |

## 六、已知限制与人工验收

- Resource Dock 的 0–500 项、键盘、ARIA、reduced-motion 和无 N+1 由组件/模型测试证明；当前没有独立的 Dock 浏览器用例。
- 旧 `artifact-flow`、Canvas shell/performance/keyboard E2E 仍绑定已删除的 PlusMenu Artifact 入口，未计入最终资源 Playwright；对应生成主链应后续改为自然语言 Turn，纯壳测试可改用 DB fixture。
- Live 的真实 Provider、Chrome/Safari 真麦克风与真人读屏措辞不由本次自动矩阵重复证明；本轮只验证浏览器产品状态机与受控外部 I/O。
- 成员撤销后的新请求会重新鉴权；已越过鉴权点的并发写请求与撤销事务的严格串行化仍需独立契约决定，本轮未修改 identity/authority 边界。
- 普通 HTTPS Markdown 远程图片仍可能产生浏览器跟踪请求，作为后续隐私硬化项记录。

## 七、Reviewer handoff

`RM_RX03_READY`。本文件不修改 RM 计划为 PASS，不替唯一审查者发出 `RM_RX03_ACCEPTED`。
