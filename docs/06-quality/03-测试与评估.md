# 测试与评测

- 状态：`draft`

## 强制规则

核心纯逻辑必须在**实现它们的同一个PR内**附带单元测试，不允许"先实现后补测"：

- Canvas协议校验（`packages/canvas-protocol`）——安全边界；
- 结构化课程状态机（转移、guard、中断栈）——显式课程模式的正确性边界；
- 掌握度计算与更新——自适应链路的事实基础。

三者都是纯函数逻辑，测试成本低、回报高。UI组件、页面和样式不作此强制。

## 测试层级

- 单元测试：统一Agent Loop、Context/Tool Policy、Profile、状态机、掌握度和Schema；
- 集成测试：PostgreSQL、Gateway持久化、模型Gateway和检索链路；
- 契约测试：前后端API、事件和Canvas Artifact；
- E2E：通用Chat、K12自由问答、结构化课程、Notebook恢复与多模态/Artifact流程；
- 负载测试：对话、检索、事件写入和长连接；
- 故障测试：模型超时、Redis失败、Worker重启和任务重试。

## 当前覆盖状态

测试数量会随分支变化，本文件不维护容易过期的计数；当前数量与通过情况以本地命令和CI运行结果为准。

| 层级                 | 当前状态                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 单元测试             | 已覆盖唯一`AgentLoopEngine`多圈工具/预算/单终态、模型流验证、Gateway Service幂等/恢复/审批等待、Client NDJSON、Telegram归一化/切分、Node遍历/escape/重放/撤销，以及Canvas、状态机、掌握度、安全Gate等纯逻辑                                                                                                                                                                                                                                                                                                                                                                                                              |
| 协议/契约测试        | 已覆盖严格`gateway.v1`角色/Envelope/Event/能力/审批/渠道/Node Schema和依赖边界；Web与TUI Fixture通过Gateway到达同一Notebook/Conversation；另覆盖Canvas信任边界、EduCanvas Web SSE、引用、版本/type/字段/资源上限与reader释放                                                                                                                                                                                                                                                                                                                                                                                             |
| CI                   | `quality`执行lint、typecheck、unit与coverage门禁；DB/Worker/Migration 集成按风险分流：`db-integration`（packages/db 改动）、`worker-integration`（apps/worker 与 packages/asset-processing 改动）、`migration-integration`（drizzle/与 Schema 定义改动，聚合历史不可变门禁、记录完整性、drift 检查与 fresh/N-1 迁移测试）；普通PR/main push的`e2e`在生产构建上运行8个Chromium核心smoke（仅浏览器可执行面触发），Runtime受影响时追加真实composition；`release-evidence`仅在证据包、校验器、Migration/发布治理变更或显式手动发布时运行。`checks`不重复执行测试，只作为唯一required聚合上下文验证本次分类要求的lane全部成功 |
| PostgreSQL集成测试   | 已覆盖Gateway迁移、User/Agent、私有/共享Notebook、contributor/viewer权限、actor审计、Operation幂等/冲突/跨租户隐藏、审批、Channel binding/Delivery去重、Node配对/心跳/调用/重放结果/撤销，以及原有事务、匿名和Artifact边界                                                                                                                                                                                                                                                                                                                                                                                               |
| 迁移应用/回滚测试    | 已有迁移应用验证；向下回退与备份恢复演练待完成                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 浏览器E2E            | `playwright.pr.config.ts`从完整稳定集显式选择8个`@smoke`，普通PR/main push只在Chromium桌面串行执行；`playwright.config.ts`保留完整稳定定义和chromium/chromium-mobile/firefox矩阵，仅由nightly或手动预发布运行。`@ui`独立视觉/设计QA只在nightly或手动任务运行Chromium+Firefox，不阻塞普通PR；完整集继续覆盖Notebook隔离、Artifact/Worker/版本、权限、SSE、恢复、键盘、性能和安全边界，不以删测试换速度                                                                                                                                                                                                                    |
| Live/Canvas联合证据  | 单元/契约层阻断delta游标、语义分段、Speech Session、PCM/字幕时钟、Artifact proposal/schema/version、provenance与沙箱预算；PR smoke固定包含`live-voice-flow`、`artifact-flow`与`canvas-resource-access`核心纵切。真实Provider canary、完整浏览器矩阵和真人主观体验不在普通PR重复运行，也不以fake证据冒充。C08仍由项目负责人签署。                                                                                                                                                                                                                                                                                         |
| 视觉回归             | 已固定桌面/移动Chat-empty、移动AI不可用、`pipeline_flow`桌面/移动/reduced-motion截图；使用reduced-motion和禁用动画稳定像素，并验证关键动画窗口无Layout Shift或长任务                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 模型、RAG与Agent评测 | 已有Provider边界、SSE/streaming、消息账本和冻结中英文K12对抗Fixture的工程测试；确定性检测器不替代人工红队，真实课程质量回归集、RAG链路和Agent教学效果评测仍待落地                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### 浏览器 CI 证据语义

| 范围       | 触发与执行                                                                   | skip / 待验证语义                                                                                 |
| ---------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `affected` | PR/main push 仅在 changed-files 判定浏览器面受影响时运行 `chromium-pr-smoke` | 未受影响时显式 `skip`；只证明该 SHA 的 Chromium smoke，不证明 Firefox、Safari 或完整稳定集        |
| `full`     | `workflow_dispatch` 显式运行全部 CI lane 与完整稳定浏览器矩阵                | 不依赖 changed-files；缺 chromium、chromium-mobile 或 firefox 任一 project 都是证据失败           |
| `nightly`  | `schedule` 显式运行全部 CI lane 与完整稳定浏览器矩阵                         | 与手动 full 分开记账；红灯记 `failed`，未产出或尚未执行记 `pending`，不能由最近一次 PR smoke 替代 |

Playwright 所有范围固定 `retries: 0`。`ci-evidence` 分别记录预期的 `run` / `skip`、实际的
`verified` / `failed` / `pending`，以及所需 project。Safari/WebKit、真实 Provider canary、真实
麦克风和真实外部服务统一保留为 `[待验证]`，fake 或本地浏览器证据不得替代。

## 本地验证

```bash
pnpm lint
pnpm typecheck
pnpm test:unit
TEST_DATABASE_URL=postgresql://educanvas:educanvas@localhost:5432/educanvas_integration pnpm test:integration
# D06 起提供按风险分流的独立入口（test:integration 保留 DB+Worker 组合语义）：
TEST_DATABASE_URL=postgresql://educanvas:educanvas@localhost:5432/educanvas_integration pnpm test:integration:db
TEST_DATABASE_URL=postgresql://educanvas:educanvas@localhost:5432/educanvas_integration pnpm test:integration:worker
TEST_DATABASE_URL=postgresql://educanvas:educanvas@localhost:5432/educanvas_integration pnpm test:integration:migrations
E2E_DATABASE_URL=postgresql://educanvas:educanvas@localhost:5432/educanvas_e2e pnpm test:e2e
E2E_DATABASE_URL=postgresql://educanvas:educanvas@localhost:5432/educanvas_e2e pnpm test:e2e:pr
```

运行集成测试和E2E前，应先创建对应测试数据库并执行迁移。两类测试都校验数据库名后缀，避免清空开发共享库或生产库；E2E串行运行并使用生产构建。

## S1 Canvas 资源访问 E2E

测试文件：`tests/e2e/canvas-resource-access.spec.ts`

- 浏览器：Chromium (Desktop Chrome)
- viewport：1280×720
- 数据隔离：使用 `E2E_DATABASE_URL` 指定的独立数据库（名以 `_e2e` 或 `_test` 结尾），仅使用合成身份、合成 Notebook、合成 Source/Artifact
- 运行命令：`rtk pnpm exec playwright test tests/e2e/canvas-resource-access.spec.ts`

| 用例                                              | 状态   |
| ------------------------------------------------- | ------ |
| 同一 Notebook Source 可以打开                     | passed |
| 同一 Notebook Artifact 可以打开                   | passed |
| Source/Artifact 使用同一 CanvasHost 宿主语义      | passed |
| Notebook A 打开的资源不会残留到 Notebook B        | passed |
| Notebook B 请求 Notebook A Source 返回统一拒绝    | passed |
| Notebook B 请求 Notebook A Artifact 返回统一拒绝  | passed |
| 不同用户不能访问对方资源                          | passed |
| Artifact 最新版本可以打开                         | passed |
| 历史版本可以选择并恢复                            | passed |
| 旧 Source/Artifact URL 仍兼容且保持 Notebook 隔离 | passed |
| 旧 PublicArtifact 判分路径仍能提交                | passed |
| trace 仅含合成身份、Notebook、Source 与 Artifact  | passed |

2026-07-28 Codex 收口复审：目标 spec 在生产 webpack 构建上重新执行，2/2 passed。
Source renderer 另有 9 条真实 TSX 组件测试和 12 条状态/fixture 测试，覆盖统一宿主、
registry Renderer、ARIA 状态、空内容、PDF/PNG 受控引用以及音视频文字替代。测试使用独立
`educanvas_e2e` 数据库、进程内 fixture Provider 与隔离对象存储根；不读取真实学生数据、
真实 Provider Key 或外部模型。

## AI评测

以下条目是接入真实模型后的评测目标，不代表当前Scripted Gateway或前端Demo Script已经通过这些评测。Demo Script只用于确定性单元测试或显式Fixture，不得作为Agent质量样本，也不进入正常学习页依赖图。

- 课程事实正确性；
- 引用是否支持回答；
- 学段表达是否合适；
- 自由问答是否避免伪造教学进度，结构化课程是否遵循状态；
- 工具选择是否正确；
- Canvas Schema成功率；
- 安全拒答和边界；
- Token、延迟和成本。

## 发布门槛

- 核心流程E2E通过；
- 数据迁移可回滚；
- 无高危安全问题；
- 检索评测不低于当前基线；
- 模型Prompt或版本变更经过回归集；
- 关键监控和告警存在。
