# 质量基线报告（Q07 收口）

> **报告元信息（可复现）**
>
> - 报告版本：`2026-08-07-1`
> - 基线：`main` @ `f825bf6`（#301，2026-08-07 02:12 UTC CI SUCCESS）
> - 事实来源：`git log origin/main`、GitHub PR/check-run 记录、`docs/plan/active/Q-质量观测成本.md` 台账
> - 生成方式：按文末「附：生成步骤」核验后人工回写；与 Q07 PR 一并提交
> - 重新生成条件：main 合入新发布门禁、评测集版本变化（绝不覆盖旧结论，见 Q 计划硬边界）、CI 结构变更，或本报告声称的任一事实发生漂移时

## 1. 已验证（已合入 main，main 回归全绿）

| 阶段            | 产物                                                               | PR   | 合入 commit | 证据                 |
| --------------- | ------------------------------------------------------------------ | ---- | ----------- | -------------------- |
| Q03 成本预算    | budget controller + ledger；General/Teaching/Artifact 分档预算模板 | #291 | `01424d4`   | 台账 DONE；入口见 §5 |
| Q04 SLO/Runbook | metrics registry + internal metrics 端点 + SLI/SLO/Runbook         | #295 | `0085e06`   | 台账 DONE；入口见 §5 |

> **历史注记**：Q03/Q04 合入于 2026-08-06（GitHub Actions 大规模故障期），其 head commit 最终 check 记录非全绿（#291 e2e failure、#295 checks failure + e2e skipped）。当前 main 完整 CI run（#31140519204，2026-08-07）8 项终态：checks/e2e/integration/runtime-pressure/secret-scan/windows SUCCESS + release-evidence SKIPPED（rc1 未发布，预期）。已验证以「当前 main 回归全绿 + 代码已合入」为准，合入期门禁记录作为历史事实保留。

## 2. 已合入未验收（代码在 main，验收未完成）

| 阶段         | 产物                                                                                                 | PR   | 合入 commit | 状态           |
| ------------ | ---------------------------------------------------------------------------------------------------- | ---- | ----------- | -------------- |
| Q02 降级观测 | 9 reason 冻结于 agent-core + retrieveHybrid 三侧分类 + `retrieval_degradations` 指标 + 集成/单测覆盖 | #289 | `a7df105`   | 台账「待验收」 |

- 合入时 e2e gate FAILURE：runtime-composition `TypeError: dbModule.getDb is not a function`（基分支过期所致，非 Q02 变更本身）；该测试在后续 main 全绿 run 中通过。
- 9 reason 的测试与指标代码已在 main；降级不断供 FTS 语义已合入。
- 待验收动作（Codex 复核时）：对 Q02 变更集单独复跑一次完整 CI 并确认全绿，或在当前 main 上完成验收后回写台账。

## 3. 已回退 / 当前降级

| 阶段         | 产物                                   | 现状                                                                                                                              |
| ------------ | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| Q01 RAG 评测 | 冻结评测集 + 可复现 harness + 基线报告 | **已回退**：#288 合入后被 #293 revert（延迟基线混入多检索器配置，与计划「hybrid-only」描述不符）；revert PR body 声明修正版将重提 |

- `tooling/evals/` 当前仅剩 `node_modules`；评测集、报告与 `tooling/evals/run.mjs` 入口不在 main。
- 直接影响：收尾检查表「RAG 有冻结质量评测」未满足（见 §7 降级 1）。

## 4. 分支完成待合并（分支 CI 全绿，待 Code Owner 审批）

| 阶段           | 产物                                                                                                                                                                            | PR   | 分支 CI 状态                                  |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | --------------------------------------------- |
| Q05 测试真实性 | coverage 阈值（telemetry 92/90/83/92、agent-core 96/94/96/97、agent-runtime 78/74/86/80）+ bundle/route size 基线 + chromium-mobile 第二环境 + @ui 独立 lane + retry/flaky 汇总 | #303 | 3 次全绿                                      |
| Q06 供应链     | action 40 位 SHA pin + dependency-review 门禁 + 容器 digest pin + migration records 门禁 + release evidence 语义 + 供应链文档                                                   | #304 | 9 项终态全绿（release-evidence SKIPPED 预期） |

- 合入前，main 尚不具备 §5/§6 中标记「待 #303/#304」的入口与门禁。
- #304 分支基于 #303 分支创建，其 diff 含 Q05 commits；#303 合并后 rebase 后 #304 仅剩 Q06 变更（PR body 已声明依赖）。

## 5. 稳定入口

| 域          | 入口                                                                                                                                                                                   | 状态                                   |
| ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| RAG 评测    | `rtk pnpm exec node tooling/evals/run.mjs`（Q 计划第八节定义，具体命令由 Q01 固定）                                                                                                    | **不可用**（Q01 已回退，待重提）       |
| 成本预算    | budget controller + ledger（Q03 代码产物）；阈值与配置说明 `docs/07-operations/01-部署与可观测性.md`                                                                                   | 可用（main）                           |
| SLO/Runbook | `docs/07-operations/01-部署与可观测性.md`「Q04 SLI/SLO 与告警 Runbook」（含 release-blocking SLO 触发条件与检查命令）+ internal metrics 端点                                           | 可用（main）                           |
| 测试矩阵    | 3 包 `vitest.config.ts` 覆盖率阈值 + `tooling/quality/bundle-size-baseline.json` + Playwright @ui lane（chromium+firefox，nightly+路径触发）+ `tooling/quality/playwright-summary.mjs` | 待 #303 合并                           |
| 供应链      | `docs/06-quality/08-供应链与发布证据.md` + ci.yml 供应链门禁（§6）                                                                                                                     | 待 #304 合并                           |
| 发布证据    | `docs/06-quality/releases/rc1/manifest.json` + `scripts/validate-evidence.mjs` + ci.yml release-evidence job                                                                           | manifest 在 main；语义校验待 #304 合并 |

## 6. 失败阈值接入发布门（已接线，不只存在于文档）

| 阈值                                                                                                               | 接入点                                                               | 状态              |
| ------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------- | ----------------- |
| coverage（telemetry 92/90/83/92、agent-core 96/94/96/97、agent-runtime 78/74/86/80，只升不降）                     | 各包 `vitest.config.ts` thresholds → CI unit job                     | 待 #303 合并      |
| bundle/route size（jsTotal 4120016B、entry 875476B，超基线 1.1× 或新增路由 HTML >300KB → fail）                    | `tooling/quality/bundle-size.mjs --root apps/web` → ci.yml build job | 待 #303/#304 合并 |
| retry/flaky 纪律（CI 下 flaky 直接标 FAILURE，禁止隐藏 retry 后通过）                                              | `failOnFlakyTests: true` + `playwright-summary.mjs` → CI Summary     | 待 #303 合并      |
| migration records（51 个迁移记录段字段完整、基线必须标归档、新迁移禁止占位）                                       | `tooling/quality/migration-records.mjs` → ci.yml checks job          | 待 #304 合并      |
| dependency review（fail-on-severity: high）                                                                        | `actions/dependency-review-action@2031cfc…` → dependency-review job  | 待 #304 合并      |
| 容器 digest（postgres 服务镜像 SHA256 pin）                                                                        | ci.yml/ui.yml 服务镜像 digest                                        | 待 #304 合并      |
| release evidence 语义（passed 需 actual>0 且 ≤limit、skipped 必须带原因、failed 即失败、整体 passed 要求全部终态） | `scripts/validate-evidence.mjs` → release-evidence job               | 待 #304 合并      |
| secret scan（gitleaks + allowlist 逐条解释注释，禁止通配符忽略）                                                   | gitleaks-action → secret-scan job                                    | main 已有         |

## 7. 当前降级清单（已知风险，面向读者）

1. **RAG 冻结评测集缺失**：Q01 已回退，`tooling/evals/run.mjs` 不可用。恢复条件：Q01 修正版重提并通过验收。此期间不得宣称「RAG 已完成」。
2. **Q02 验收未完成**：合入时 e2e 门禁为 FAILURE（基分支过期）；main 已回归全绿，但 Q02 变更集无独立的全绿验收记录。
3. **Q05/Q06 门禁未合入 main**：合并前 main 无 coverage/bundle/供应链/迁移发布门；供应链加固只存在于 #304 分支。
4. **main 2026-08-06 两次 CI 失败（#285/#300）**：根因为 GitHub Actions 故障期 action 下载 `Service Unavailable`，非代码回归；2026-08-07 起恢复全绿。

## 8. 复核与归档

- **复核**：本报告与台账回写待 Codex 独立复核。Q 计划各阶段的验收判定与 PASS 声明不替 Codex 代答。
- **归档**：复核通过后按 `docs/plan/active/README.md` 生命周期执行——归档至 `docs/plan/completed/`（`2026-08-质量观测成本.md`）、更新 active 索引、勾选收尾检查表第 8 项。
- 收尾检查表（计划文档第十节）当前勾选状态见 Q07 提交的台账更新。

## 附：生成步骤（可复现）

```bash
# 1. 基线
git fetch origin main && git rev-parse origin/main

# 2. main 全绿验证（8 项终态：7 SUCCESS + release-evidence SKIPPED 预期）
gh run list --branch main --workflow CI --limit 1 --json databaseId,conclusion
gh run view <databaseId> --json jobs --jq '.jobs[] | "\(.name):\(.conclusion)"'

# 3. 各阶段合入 commit 与 check 记录
gh pr view <n> --json headRefOid,mergedAt,title
gh api repos/Timcai06/EduCanvas/commits/<headRefOid>/check-runs --jq '.check_runs[] | "\(.name):\(.conclusion)"'

# 4. 回写本文元信息（版本/基线/日期）与计划台账后，按 §5/§6 核对入口与门禁是否漂移
```
