# LC 联合收口自动化证据（2026-08-12）

- 状态：`accepted`
- 范围：X01-X03 自动化证据；C08 真人 Canvas 产品验收不在本文冒充完成

## X01 Live × Canvas

- Live 直接消费 canonical Assistant delta；语义分段与朗读净化测试阻止 fenced/inline code、公式、长 URL、Markdown 链接目标和原始 JSON 进入 TTS。
- Live 的工具与 Artifact 列表显示 `执行中/失败/已完成` 和 `生成中/生成失败/已归档/已生成` 真实状态；就绪 Artifact 通过同一 CanvasResource gate 和 Renderer Registry 打开。
- `live-voice-flow.spec.ts` 继续验证同一消息/Turn、上下文快照、插话和播放连续性；真实 Provider 与主观自然度使用 L08 负责人证据，不由 fake 测试替代。

## X02 多模态 provenance

- General Turn 从已鉴权、已物化且实际进入 Prompt 的 Asset plan 生成 Artifact provenance；模型与浏览器不能自行声明来源。
- 生成任务冻结 `assetId + versionId + representation identity`，CanvasResource 同时保留兼容 `sourceResourceIds` 与精确 `sourceReferences`。
- Provenance 只是审计/导航元数据。打开 Source、Artifact 或派生文件仍重新校验当前身份、Notebook、Asset/Version 和 Membership；跨 Notebook/用户统一 404。

## X03 CI 路由目标

- 仅当必要变更触发 E2E（减少文档噪音）；保持 `changes`/`checks` 最小化成本。
- Live/Canvas/C01-C07 关键链路变更必须进入 PR smoke。
- 保留文档/评估说明为无损触发（不触发昂贵矩阵）。

## 证据入口

- `tooling/quality/ci-impact.mjs`
- `tooling/ci-impact.test.mjs`
- `tooling/e2e-suite-routing.test.mjs`
- `playwright.pr.config.ts`
- `.github/workflows/ci.yml`

## CI 自动化核验结论

- 常规文档改动（如 `docs/**/*.md`）返回 `checks=false,e2e=false,...`，满足“无关 docs 不重复跑高成本矩阵”；发布证据文档除外会按证据路径触发 `release_evidence`。
- Live 与 Canvas 关键路径（`apps/web/features/voice/live-voice-panel.tsx`、`apps/web/features/canvas/viewer.tsx`、`packages/canvas-protocol/src/artifacts/mind-map.ts`、`tests/e2e/hydration.spec.ts`）保持 `e2e=true`。
- PR smoke 约束文件集为固定可控列表，已显式包含 `artifact-flow.spec.ts`（思维导图/Canvas）、`live-voice-flow.spec.ts`（Live）和 `canvas-resource-access.spec.ts`（Canvas 资源闭包验证）。
- `playwright.pr.config.ts` 与 CI 分流策略继续保持 `chromium-pr-smoke` + `@smoke`。

## 本地验证入口

- `rtk node --test tooling/ci-impact.test.mjs tooling/e2e-suite-routing.test.mjs`
- `rtk pnpm --filter @educanvas/canvas-protocol test`
- `cd apps/web && rtk pnpm exec vitest run server/platform/general-artifact-tool.test.ts server/canvas/resource-access.test.ts features/voice/playback/semantic-segmentation.test.ts features/voice/live-voice-resource-preview.test.tsx`

## 人工边界

- L08 已记录 Chrome/Safari 真麦克风与真实 Provider 证据；未测量的精确延迟不补造。
- C08 仍需负责人验证三类真实生成、大型 mind map、窄屏/键盘、失败/未知/恶意/超限/取消和历史回放。
- X04 的最终全量门禁与计划归档只能在 C08 签署后运行一次。
