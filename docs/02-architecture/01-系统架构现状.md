# 系统架构现状

- 状态：`accepted`
- 负责人：项目负责人
- 最后验证时间：2026-08-13（CA 本地候选；最终集成仍由 reviewer 执行）
- 内容边界：只记录已落地并由代码或测试支持的事实；稳定形态见[第二代架构](03-第二代架构.md)
- 关键决策：[ADR-0001](../09-decisions/0001-以教育能力为核心的个人智能体平台.md)、[ADR-0002](../09-decisions/0002-网关客户端渠道与能力节点.md)、[ADR-0003](../09-decisions/0003-统一运行时与笔记本上下文.md)、[ADR-0005](../09-decisions/0005-模块化单体产物与持久任务.md)、[ADR-0020](../09-decisions/0020-Personal与Notebook长期记忆边界.md)、[ADR-0021](../09-decisions/0021-模型能力独立Provider与继承规则.md)、[ADR-0025](../09-decisions/0025-语音双入口与云端级联边界.md)、[ADR-0026](../09-decisions/0026-多模态输入原件与派生表示边界.md)、[ADR-0027](../09-decisions/0027-Canvas多形态输出与交互运行时边界.md)

## 一、已成立的产品与数据边界

- 一个自然人拥有一个长期 Personal Agent；Web、TUI 和外部渠道是不同入口，不是不同 Agent；
- Notebook 是 Source、Conversation、Artifact 和共享 Membership 的聚合根；
- Gateway 是身份、路由、Operation、审批、事件恢复和渠道投递的控制平面；Model Gateway 只适配模型供应商；
- PostgreSQL 是身份、Notebook、消息、Operation、Artifact、学习事实和持久任务的事实源；
- 教育判分、掌握度和结构化课程状态由确定性代码维护，不能由模型或浏览器直接写成事实；
- 当前保持模块化单体：可运行组合根位于 `apps/`，可复用协议、运行时和领域能力位于 `packages/`。

Canvas 是跨 Source/Artifact 的统一工作面，不是 Notebook 的事实源。Source、Artifact 和运行记录仍由各自仓储持有；稳定边界见[统一 Canvas 工作面](04-统一画布工作面.md)。独立 Web Runtime、Experiment Runtime 及 TUI/Telegram 安全降级已实现；生产 Experiment Artifact 数据源仍未完成。Web Live Voice 已有本地 fake-provider 与自动化证据；本轮没有重新验证真实 Provider、Safari 或真人麦克风，不能把它们写成 CA 最终候选证据。

RM 接受候选 `ae7c82276c4d44fa2d7821947513eb5e3a4db4b5` 的只读审计确认：Artifact 以
immutable version 与 owning version job 保存 provenance；浏览器 Context Snapshot 只是选择事实，
服务端仍按 effective subject、Notebook、ready version 重新物化与鉴权；`allowedActions` 和资源打开
每次按服务端事实重算，provenance 本身不授权；普通 Turn 与 Live 使用同一资源摘要投影。这些是
两线接缝输入，尚不表示 RM 已合并进 CA 候选。

K12 消息在 CA08B 后把“可见事实”和“教学运行态”显式分层：`legacy|observe` 从
`chat_messages` 读取，`platform` 只有在全量 missing/mismatch/orphan 均为零时才从
`conversation_messages` 投影可见字段；公开 message ID、引用、分页键以及 lease、heartbeat、
cancel、recovery 继续保留 legacy 身份和运行态。provenance sidecar 冻结确定性一对一映射，任何
缺失或作用域漂移整页 fail closed。回退是 `legacy` 配置加进程重启，不删除旧表。

[完整架构总览（SVG）](05-系统架构总览.svg)用于快速理解入口、控制面、Agent Runtime、可信领域服务与持久任务之间的关系；本页文字和已接受 ADR 仍是可检索的事实依据。

## 二、当前进程与入口

```mermaid
flowchart TB
    Web["Web\nNext.js UI + BFF/SSE"]
    TUI["TUI"]
    Channel["Channel Adapter\nTelegram experimental"]
    Node["Capability Node\nallowlisted read"]

    Web --> WebGeneral["Web General BFF\nGateway compatibility"]
    Web --> WebTeaching["Web Teaching Turn"]
    Web --> Voice["Voice\nDictation HTTP / Live ASR+TTS WS"]
    WebGeneral --> TurnApp
    WebTeaching --> TurnApp

    TUI --> Gateway["Gateway Control Plane"]
    Channel --> Gateway
    Node --> Gateway
    Gateway --> GatewayRunner["Gateway Agent Runner"]
    GatewayRunner --> TurnApp["TurnApplicationService"]
    TurnApp --> Loop["AgentLoopEngine"]

    Loop --> Model["Model Gateway"]
    Loop --> Tools["Tool Kernel"]
    MCP["MCP v1\nStreamable HTTP"] --> Tools
    Gateway --> DB[(PostgreSQL)]
    Voice --> Gateway
    WebGeneral --> DB
    WebTeaching --> DB
    Tools --> DB
    WebTeaching --> Worker["Durable Worker"]
    Worker --> DB
```

Web 浏览器聊天仍使用 `/api/v1/chat/*` 与 `/api/v1/learn/*` 的 BFF/SSE 兼容层；Dictation 经同源 HTTP 调用批量 ASR，Live 的 ASR/TTS 经短时单次 ticket 进入 Gateway WebSocket。TUI、Channel 和 Node 通过独立 Gateway 进程。浏览器聊天协议没有被强制改成 Gateway 的持久 NDJSON 协议，这是 ADR-0002 的兼容选择。Gateway、Web General 与 Web Teaching 的服务端执行语义已经收敛到同一个 Turn Application；Live ASR final 只提交一次现有 Turn，Assistant delta、工具状态、来源和 Artifact 继续写同一消息与账本。Node 的 L0/L1 只读 Adapter与MCP v1无鉴权L0/L1 Adapter已在Gateway和Web General接入。Gateway Asset、高风险MCP续跑和真实Credential Broker仍在后续阶段。

## 三、当前模块职责

| 模块                                          | 当前职责                                                                                     | 当前限制                                     |
| --------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------- |
| `packages/gateway-core`                       | `gateway.v1` 的角色、Envelope、事件、能力、审批、渠道、Node、投递和引用 Schema               | 不依赖 Next.js、Drizzle、K12 或 Provider SDK |
| `packages/gateway-runtime`                    | 路由、指纹、幂等、Operation 事件流、恢复、审批续跑和单终态                                   | 不承担Profile、Tool或领域逻辑                |
| `apps/gateway`                                | HTTP组合根、Client/Node session、目录、审批、NDJSON、Node/MCP Tool与内部指标                 | Asset与K12 Profile尚未接入                   |
| `packages/agent-runtime`                      | 唯一目标`TurnApplicationService`、`AgentLoopEngine`、Context Engine、Tool Kernel和模型流验证 | 通过Port接收可信归属，不拥有K12事实          |
| `apps/web`                                    | 学生 Web、BFF/SSE、通用与教学 Profile 组合、兼容事件投影                                     | 浏览器仍使用兼容 SSE，不承担 Operation 终态  |
| `apps/tui`                                    | Gateway bootstrap、会话、流式聊天、恢复、审批、渠道与 Web handoff                            | 不直接访问数据库或模型 Runtime               |
| `packages/channel-telegram` / `apps/telegram` | 私聊文本归一化、绑定、幂等、Gateway 调用和投递账本                                           | 实验性、非默认启动，无 live 凭据证据         |
| `packages/node-runtime`                       | 把私人 Node 的新鲜能力、调用等待与结果限制适配到统一 Tool Kernel                             | 只提供既有 L0/L1 只读能力                    |
| `packages/mcp-runtime`                        | MCP v1可信注册、SDK生命周期、Schema防漂移、Credential Port、输入输出限额与脱敏健康状态       | 只开放L0/L1；Bearer缺Broker时禁用            |
| `packages/node-host` / `apps/node`            | 出站配对、心跳、撤销、状态与白名单只读文件                                                   | 不开放 Shell、写文件、任意根目录或入站端口   |
| `packages/teaching-core`                      | 教育状态、guard、画像/目标图、确定性诊断、掌握度和可信学习事件纯逻辑                         | 不负责模型循环                               |
| `packages/teaching-runtime`                   | K12 Prompt、安全、课程 Workflow、领域回调与 Teaching Tool Adapter                            | 不拥有独立Agent Loop或Tool生命周期           |
| `packages/db/src/schema/study.ts`             | 最小画像、Notebook Goal/Objectives与诊断Attempt/Responses持久事实                            | 不保存答案键、人格标签或模型推断             |
| `apps/worker`                                 | Artifact、资料摄取和匿名主体维护的持久任务                                                   | 不承担交互连接、身份路由或代码沙箱           |

`apps/ + packages/` 顶层结构没有过时。当前问题发生在内部应用服务和运行时职责重复，不需要用全仓目录重排掩盖。

## 四、一次 Turn 的真实路径

当前三条生产 Turn 调用路径都经 `createTurnApplication` 进入
`TurnApplicationService`；具体服务只能在 `packages/agent-runtime` 的 factory 内构造。
Web General / Teaching 通过 `apps/web/server/turn-composition.ts` 共享账本、Tool Kernel 与
Trace 装配，Gateway 通过 `apps/gateway/src/turn-composition.ts` 装配自身 Adapter；入口文件只做
可信请求和事件投影。旧教学 Orchestrator 和 Tool Executor 已删除。生产应用路径现为：

1. `apps/gateway/src/agent-runner.ts`：把可信路由投影到`TurnApplicationService`并投影事件；独立Gateway Profile已接入私人Node只读工具与MCP v1无鉴权L0/L1工具，Asset仍未接通；
2. `apps/web/server/gateway/web-turn.ts`：Web BFF先建立Gateway Operation，再把通用请求投影到同一`TurnApplicationService`；Web Profile已接入结构化Asset Context、网页Tool Kernel、引用、取消和统一账本；
3. `apps/web/server/teaching/learning-turn.ts`：Web教学 Profile 把 K12 Prompt、输入/输出安全门、课程状态能力白名单和教材引用接入同一服务；教学可见消息与租约仍写 K12 表，Context/Model/Tool/Effect 审计写统一 Operation 账本。

Gateway、Web General与Web Teaching已共享应用服务、Context/Model账本和消息/Operation单写纪律；Gateway与Web General还共享同一`node-runtime`工具定义和等待语义，但网页与Asset Adapter仍是Web专用。`turn-engine.ts`负责单次模型运行事件校验，不是另一套Agent Loop。

跨入口合规fixture与可信路由/Tool Policy测试共同证明Web、TUI和Gateway保持
同一Notebook/Conversation、服务端Profile来源、五维授权、审批等待、取消和唯一终态。
入口仍可拥有不同Adapter和展示能力：Gateway Asset/K12尚未接通会明确返回
`CAPABILITY_UNAVAILABLE`，不会复制Loop或静默降级。当前不存在`steer`协议，
中途改写意图使用`cancel + contextual resend`。

## 五、当前工具与审计双轨

- Web General网页工具、Web Teaching教学工具以及Gateway/Web General的Node和MCP工具都通过生产`ToolKernel`执行；三条组合路径通过共享Resolver解析服务端五维Tool Policy，Teaching state只收窄Profile维，transport/render manifest不授予工具；旧通用注册表与教学执行器已删除，本地/教学模块只保留定义和Adapter；
- MCP工具的模型名、描述、capability、risk、effect和输入Schema来自服务端静态注册；远端`annotations`不授予权限。SDK结果先经过Schema防漂移和有界净化，远端正文、URL、Token和原始异常不进入状态、账本或模型；
- `ToolKernel` 与 PostgreSQL `tool_effects` 已实现五维授权交集、L2/L3审批门、超时/取消、幂等和结果未知语义；网页读取的Adapter函数名与公共能力名分离，Provider使用`fetchWebPage/webSearch`，权限和公共事件使用`web.fetch/web.search`；
- Gateway Operation Event 是跨入口控制面事实；Gateway迁移后由事件循环独占Operation终态，Turn Application只结算消息；
- Turn Application 在消息与引用事务内冻结最小 durable terminal intent；Gateway 在同一
  operation-event advisory lock 下对 begin/append/list/describe/recent/cancel 统一收敛。响应丢失、
  重启或重复请求不会运行第二次模型/工具，也不会产生第二终态；`legacy-disabled` 是显式回退，
  非默认行为；
- `model_runs`、`tool_calls`和`turn_context_snapshots`已additive支持通用Operation；三条生产路径均写统一Context与Model Run，Web General写网页Tool/Effect，Web Teaching写教学Tool并继续保留独立的K12安全与引用证据；
- Artifact/资料摄取等分钟级工作由 PostgreSQL 背书的 Worker 处理，不在 HTTP Turn 内等待。

剩余差异是 Profile 和入口能力差异，不再是第二套 Tool Runtime。有限收口项见[第二代架构](03-第二代架构.md)与 active plan。

## 六、身份与 Notebook 边界

- `platform_users` 与 `personal_agents` 表达一人一 Agent；
- `notebook_memberships` 的 `owner/editor/contributor/viewer` 控制 Notebook 行为，viewer 不能回复；
- Operation 保存真实 `actor_user_id`、`agent_id`、`notebook_id` 和 `conversation_id`；
- `delegated_grants` 表达显式、到期、可撤销的教师/家长/管理员授权，禁止冒充；
- Web 匿名 Cookie 只映射到 `anonymous_compat` 主体，不能成为 TUI/渠道的正式认证机制；
- 私人 Memory、Credential、Node Pairing 与默认 Tool Grant 不随 Notebook Membership 传播是设计不变量。Node的Operation级能力解析与入队已有跨Actor集成fixture；Memory、Credential与default grant仍待完整端到端fixture。

## 七、当前部署与已知缺口

本地 `make dev` 启动 Web、Gateway、Worker 和 PostgreSQL；公共 Client/Node transport 只有配置足够长度的 bootstrap token 与 session secret 才开放，内部 transport 也默认关闭。

当前不具备 production 声明条件：正式 IdP、自助账号生命周期、外部 Trace/SLO、对象删除闭环、原生多模态、长期学习者记忆、高风险MCP/Node审批续跑、真实Credential Broker、Telegram live凭据验证和微信/QQ Adapter仍未完成。Gateway bootstrap token是管理员或本地建联凭据，不是最终用户登录方案。

详细协议与入口见 [Gateway 与多入口](02-网关与多入口.md)；稳定编排术语与边界见 [Agent 编排边界](../03-ai/01-智能体编排边界.md)。

### Canvas 当前能力与目标的边界

Source 与 Artifact 已通过统一 `CanvasResource`、Renderer Registry 和服务端授权投影进入 Canvas；PDF、图片、文档、文本、音频和视频来源具有受控预览，结构化 Artifact、媒体产物和独立 origin/process 的持久 Web Runtime 已落地。浏览器端仍不得执行未经注册的模型代码。

Experiment Runtime 契约、受控 CPU Adapter、有界 Renderer、跨入口交接和实时语音链路
均已按竞赛级范围完成。U16 的 fixture 仍不是生产 Experiment Artifact 数据源；真人
麦克风、课堂噪声、十分钟长流与受控并发仍是非阻塞后续，不能宣称已经验证。统一
Canvas 的稳定边界见 [ADR-0009](../09-decisions/0009-统一画布工作面与运行时分层.md)与
[UV 归档](../plan/completed/UV-画布语音.md)。
